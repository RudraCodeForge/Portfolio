# Portfolio Backend

## Setup

```bash
npm install
copy .env.example .env
npm run dev
```

API health check: `GET http://localhost:5000/api/health`

Set `MONGO_URI` in `.env` before starting the server.
